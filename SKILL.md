---
name: pp-repo-trust
description: This skill should be used when the user wants public, verifiable security signals on a GitHub repo - CI scanning with Trivy (vulnerabilities, secrets, licenses), a CycloneDX SBOM attached to every release, and an OpenSSF Scorecard badge - or wants the current state of those signals audited and improved. Trigger on "pp-repo-trust", "repo trust", "security badges", "badges de seguridad", "scorecard", "repo SBOM", "SBOM del repo", "prepare the repo for publishing", "preparar el repo para publicar", "make the repo trustworthy", "que el repo dé confianza", "repo audit", "auditoría del repo", "repo security status", "estado de seguridad del repo", or requests to make a repository's security posture visible to downloaders.
---
Every invocation runs the three-phase flow in references/SPEC.md §5.0: **A** install what's missing (§0 preflight, then §1 deliverables; no-op when installed), **B** operate (live reads of workflow, Scorecard, release/SBOM state — never from memory), **C** advise (map findings to at most 3 concrete recommendations via the §5.0 table). Emit every report in the §5.1 schematic format. Resolve every <RESOLVE> item against official sources before writing files; never from memory. Reply in the language the user invoked the skill in.
